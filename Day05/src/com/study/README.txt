1. 定义int类型的数组arr, 存储元素11, 22, 33, 44, 55, 通过for循环, 遍历数组. (要求在控制台中在一行显示,显示格式为:11, 22, 33, 44, 55 )

2. 已知数组int[] arr = {5, 15, 2000, 10000, 100, 4000};, 求数组所有元素中的最小值.

3. 键盘录入3个整数, 并将它们都存入到数组中, 然后获取所有元素之和, 并打印结果.

4. 定义int类型的数组, 存储数据: 11, 33, 22, 55, 44, 对数组元素进行反转, 并打印反转后的结果.

5. 在编程竞赛中, 有6个评委为参赛的选手打分, 分数在0~100之间.
   假设小明参加比赛后, 评委对其进行打分, 求小明的平均分是多少, 并将结果打印到控制台上.
   //提示: 选手的最后得分为: 去掉(一个最高分 + 去掉一个) , 然后和4取余即可.
   /*
    打印格式:
        请录入第一个评委的分数:
        90
        请录入第二个评委的分数:
        100
        ...  //这里要接收6个教练的评分.

        选手的最终得分情况为: 去掉最高分**, 去掉最低分**, 求出平均分为: **
   */

 6. 已知序列1, 1, 2, 3, 5, 8, 13..., 求第12个数字是多少.



 7.已知一个数组 arr = {19, 28, 37, 46, 50}; 键盘录入一个数据。完成查找该数据在数组中的索引，并在控制台输出找到的索引值。如果不存在请输入-1

 8.有这样的一个数组，元素是{68,27,95,88,171,996,51,210}。求出该数组中满足要求的元素和，要求是：求和的元素个位和十位都不能是7，并且只能是偶数。
 -------------------------------------------------------------------------
 选做题:

 9. 随机生成10个[1,100]不重复的整数, 存入长度为10数组中, 最终遍历数组.

 10. 基于第9题, 生成两个数组, 将数组的交集部分打印到控制台上.

 11. 定义一个数组其中包含多个数字。用自己的方式最终实现，奇数放在数组的左边，偶数放在数组的右边。（可以创建其他数组，不必须在原数组中改变）

